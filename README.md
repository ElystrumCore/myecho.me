# Echo

Echo is a living journal platform built with FastAPI and a Vite/React dashboard.

Core backend code lives in `echo/`, the frontend lives in `frontend/`, and the
product/architecture notes for this phase are documented in `echo.md`.
